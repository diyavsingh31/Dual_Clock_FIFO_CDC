# Dual_Clock_FIFO_CDC
Designed and implemented a parameterizable asynchronous dual-clock FIFO in SystemVerilog for reliable data transfer between independent clock domains. Implemented Gray-coded read/write pointers, two-flop CDC synchronizers, full/empty flag generation and dual-port memory architecture. 
